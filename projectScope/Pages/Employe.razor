@page "/Employees"

@using projectScope.Data;
@inject EmployeeService EmployeeService


<h5>Enter your info</h5>
<hr />
<!--
<div class="container">
    <div class="row ">
    </div>
   
        <div class="col-sm-12 col-lg-12 col-md-12 my-3">
            <MudButton Variant="Variant.Filled">Default</MudButton>
            <MudButton Variant="Variant.Filled" Color="Color.Primary">Primary</MudButton>
            <MudButton Variant="Variant.Filled" Color="Color.Secondary">Secondary</MudButton>
            <MudButton Variant="Variant.Filled" Disabled="true">Disabled</MudButton>
        </div>
        <!--
        <div class="col-lg-12  pl-3">
            <EditForm class=" my-2" Model="@newTask" OnValidSubmit="AddTask">
                <br />
                <label>First Name</label>
                <InputText class="form-control mx-2" @bind-Value="newTask.FirstName" />
                <br />
                <label>Last Name</label>
                <InputText class="form-control mx-2" @bind-Value="newTask.LastName" />
                <br />

                <label>City</label>
                <InputText class="form-control mx-2" @bind-Value="newTask.City" />
                <br />

                <label>Adress</label>
                <InputText class="form-control mx-2" @bind-Value="newTask.Adress" />
                <br />

                <label>Salary</label>
                <InputText class="form-control mx-2" @bind-Value="newTask.Salary" />
                <br />

                <button class="btn btn-outline-primary" type="submit">Add Form</button>
            </EditForm>
        </div>

        <br />
        <hr />
        <div class="col-6">
            <div style="max-width: 400px;">
                <MudCard>
                    <MudCardContent>
                        <MudForm @ref="form" @bind-IsValid="@success" @bind-Errors="@errors" @OnValidSubmit="AddTask">
                            <MudTextField T="string" Label="First name" Required="true" RequiredError="First name is required!" @bind-Value="newTask.FirstName" />
                            <MudTextField T="string" Label="Last name" Required="true" RequiredError="Last name is required!" @bind-Value="newTask.LastName" />
                            <MudTextField T="string" Label="City" Required="true" RequiredError="City is required!" @bind-Value="newTask.City" />
                            <MudTextField T="string" Label="Addres" Required="true" RequiredError="Addres is required!" @bind-Value="newTask.Adress" />
                            <MudTextField T="string" Label="Salary" Required="true" RequiredError="Salary name is required!" @bind-Value="newTask.Salary" />



                            <MudCheckBox T="bool" Required="true" RequiredError="You must agree" Class="ml-n2"
                                         Label="I agree that !" />
                        </MudForm>
                    </MudCardContent>
                    <MudCardActions>
                        <MudButton Variant="Variant.Filled" Color="Color.Primary" Disabled="@(!success)" Class="ml-auto" OnClick="AddTask">Add</MudButton>
                    </MudCardActions>
                </MudCard>

                <MudExpansionPanels>
                    <MudExpansionPanel Text="@($"Show Errors ({errors.Length})")">
                        @foreach (var error in errors)
                        {
                            <MudText Color="@Color.Error">@error</MudText>
                        }
                    </MudExpansionPanel>
                </MudExpansionPanels>
            </div>

        </div>




        <div class="col-6">
            <MudSimpleTable Hover="true" Dense="true" FixedHeader="@fixedheader" Style="@(fixedheader?"height:300px;":"")">
                <thead>
                    <tr>

                        <th>First Name</th>
                        <th>Last Name</th>
                        <th>City</th>
                        <th>Adress</th>
                        <th>Salary</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var Info in InfoList)
                    {
                        <tr>


                            <td>@Info.FirstName</td>
                            <td>@Info.LastName</td>
                            <td>@Info.City</td>
                            <td>@Info.Adress</td>
                            <td>@Info.Salary</td>
                        </tr>
                    }
                </tbody>
            </MudSimpleTable>
            <MudSwitch @bind-Checked="@fixedheader">Fixed Header</MudSwitch>

            <hr />
        </div>
    </div>
    <hr />
    <div class="row">
        <h3>Pia Chart</h3>
    </div>
</div>
    -->
@code {

    /*  MudTextField<string> pwField1;
      MudForm form;*/



}
